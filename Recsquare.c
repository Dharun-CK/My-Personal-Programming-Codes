#include<stdio.h>
#include<math.h>
int main()
{
	int sq(int);
	int num,i;
	printf("Enter The Num:");
	scanf("%d",num);

return 0;	
}


int sq(int x)
{
    while(x!=0)
	int x = pow(x,2)+(x-1);
	return x;
}